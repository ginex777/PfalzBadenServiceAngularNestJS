import { BenachrichtigungenModule } from './benachrichtigungen.module';

describe('BenachrichtigungenModule', () => {
  it('should be defined', () => {
    expect(BenachrichtigungenModule).toBeDefined();
  });
});
