// ============================================================
// PBS — API Service barrel export
// All feature facades use ApiService directly.
// The feature-specific services below are dead code and should
// be deleted: kunden-api, rechnungen-api, angebote-api,
// buchhaltung-api, einstellungen-api, belege-api, audit-api
// ============================================================
export { ApiService } from './api.service';
